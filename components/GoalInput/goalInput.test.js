import 'react-native';
import React from 'react';
import App from '../App';
import renderer from 'react-test-renderer';

describe('Add button', () => {
  it('should add one item to the state', () => {});
});

describe('Modal', () => {
  it('should show the modal input screen on click', () => {});
});
